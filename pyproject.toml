[tool.ruff]
# Максимальная длина строки (стандарт PEP8 - 88, многие используют 100 или 120)
line-length = 79
# Версия Python, под которую пишется код
target-version = "py311"

[tool.ruff.lint]
# Включаем базовые проверки (E, F) и проверку импортов (I)
select = ["E", "F", "I"]